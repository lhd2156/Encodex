apiVersion: v1
kind: Service
metadata:
  name: encodex-service
  namespace: default
spec:
  selector:
    app: encodex
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30180
  type: NodePort